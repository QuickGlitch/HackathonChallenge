{
  "name": "store-hackathon",
  "version": "1.0.0",
  "description": "Pentesting application - Simple web shop with Vue.js frontend and Express.js backend",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:migrate": "turbo run db:migrate",
    "db:reset": "turbo run db:reset",
    "db:setup": "turbo run db:setup",
    "docker:up": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:up:prod": "docker-compose -f docker-compose.yml up",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "turbo": "^2.6.0"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
