BASE_URL=http://localhost
DOMAIN=localhost

# Note: Services inside Docker (boomer bots) use HOST http://traefik for inter-container communication
# The BASE_URL and DOMAIN here are only used for external access and service labels.
