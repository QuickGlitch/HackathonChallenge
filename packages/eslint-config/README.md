# @store-hackathon/eslint-config

Shared ESLint configuration for the store-hackathon monorepo.

## Usage

In your app's `eslint.config.js`:

```js
import sharedConfig from '@store-hackathon/eslint-config';

export default [
  ...sharedConfig,
  // your overrides here
];
```
